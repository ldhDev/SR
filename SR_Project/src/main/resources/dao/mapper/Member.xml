<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-/mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.mapper.MemberMapper">
	<select id="exist" resultType="int" parameterType="map">
		select count(*) from member where user_id=#{id}
	</select>
	<select id="imformation" resultType="Member" parameterType="map">
		select * from member where user_id=#{id}
	</select>
	
	<select id="namecheck" resultType="Member" parameterType="map">
		select name from member where name = #{name}
	</select>
</mapper>
